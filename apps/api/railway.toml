# Railway configuration for Sidra API (NestJS)
# Using Docker for explicit build control

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
