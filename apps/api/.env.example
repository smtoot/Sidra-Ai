# Server Configuration
PORT=4000

# SECURITY: JWT Secret
# CRITICAL: This MUST be changed in production!
#
# Generate a strong secret using:
#   openssl rand -base64 64
#
# IMPORTANT NOTES:
# 1. NEVER commit the real secret to git
# 2. Use different secrets for dev/staging/production
# 3. Rotate secrets periodically (invalidates all tokens)
# 4. Store securely (AWS Secrets Manager, environment variables)
#
# Changing this value will LOG OUT all users
JWT_SECRET="CHANGE_ME_IN_PRODUCTION"

# SECURITY: CORS Configuration
# Comma-separated list of allowed origins (no spaces)
# Development example:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3002
# Production example:
# ALLOWED_ORIGINS=https://app.sidra-ai.com,https://www.sidra-ai.com

# Database Configuration (if not using default)
# DATABASE_URL="postgresql://user:password@localhost:5432/sidra_db"

# Email Configuration (SendGrid)
# SENDGRID_API_KEY="your-sendgrid-api-key"

# Cloudflare R2 Configuration (recommended for file uploads)
# R2 is S3-compatible with zero egress fees
# See: https://developers.cloudflare.com/r2/
#
# R2_ACCOUNT_ID="your-cloudflare-account-id"
# R2_ACCESS_KEY_ID="your-r2-access-key"
# R2_SECRET_ACCESS_KEY="your-r2-secret-key"
# R2_BUCKET_NAME="sidra-production"
# R2_PUBLIC_URL="https://pub-xxx.r2.dev"  # Optional: for public bucket access

# AWS S3 Configuration (legacy - use R2 instead)
# AWS_ACCESS_KEY_ID="your-access-key"
# AWS_SECRET_ACCESS_KEY="your-secret-key"
# AWS_REGION="us-east-1"
# AWS_S3_BUCKET="sidra-uploads"

# Local File Storage (fallback when cloud storage not configured)
# UPLOAD_LOCAL_PATH="/path/to/uploads"
